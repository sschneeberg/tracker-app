<!DOCTYPE html>
<html
    lang="en"
    xmlns="http://www.w3.org/1999/xhtml"
    xmlns:fb="http://ogp.me/ns/fb#"
>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta
            name="image"
            property="og:image"
            content="http://periodtrackerapp.herokuapp/landing.png"
        />
        <meta name="title" property="og:type" content="website" />
        <meta
            name="description"
            property="og:description"
            content="A gender neutral, user driven period tracker app"
        />
        <title>Tracker</title>
        <!-- Materialize CSS -->
        <link
            rel="stylesheet"
            href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"
        />

        <link rel="stylesheet" href="/css/style.css" />
    </head>

    <body>
        <header>
            <nav>
                <div class="nav-wrapper">
                    <% if(!currentUser) { %>
                    <a href="/" class="brand-logo">Tracker App</a>
                    <% } else { %>
                    <a
                        href="/user/<%= (new Date().getMonth() + 1) %>"
                        class="brand-logo"
                        >Tracker App</a
                    >
                    <% } %>
                    <ul id="nav-mobile" class="right">
                        <% if (!currentUser) { %>
                        <li>
                            <a class="modal-trigger" href="#signup">Signup</a>
                        </li>
                        <li>
                            <a class="modal-trigger" href="#login">Login</a>
                        </li>
                        <% } else { %>
                        <li>
                            <a
                                href="#"
                                data-target="slide-out"
                                class="sidenav-trigger"
                                ><i class="fas fa-bars"></i
                            ></a>
                        </li>
                        <li>
                            <a href="/auth/logout">Logout</a>
                        </li>
                        <% } %>
                    </ul>
                </div>
            </nav>
        </header>
        <% if (currentUser) { %> <%- include('partials/sidenav') %> <% } %> <%-
        include('partials/alerts') %> <%- body %>

        <script
            src="https://kit.fontawesome.com/3c951815da.js"
            crossorigin="anonymous"
        ></script>
        <!-- Materialize JavaScript -->
        <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
        <script src="/js/app.js" type="text/javascript"></script>
    </body>
</html>
